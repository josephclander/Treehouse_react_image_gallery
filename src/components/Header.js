import React from 'react';
import Form from './Form';
import Nav from './Nav';

const Header = () => {
  return (
    <div className='header'>
      <Form />
      <Nav />
    </div>
  );
};

export default Header;
